# JavaScript - DOM Manipulation

Holberton School project covering JavaScript DOM manipulation: selecting elements, event listeners, modifying classes and text content, creating elements, and using the Fetch API to consume external APIs.

